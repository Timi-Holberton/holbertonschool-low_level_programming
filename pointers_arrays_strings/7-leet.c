#include "main.h"
#include <stdio.h>

/**
 * leet - check the code for
 * @tab : tableau
 * Return: char
 */

char *leet(char *tab)
{
	int i;

	for (i = 0; tab[i] = '\0'; i++)
	{
		if ((tab[i] >= 65) && (tab[i] <= 90))
			i++;
	}
	return (tab);
}
