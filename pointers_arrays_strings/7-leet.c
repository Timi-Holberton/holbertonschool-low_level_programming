#include "main.h"
#include <stdio.h>

/**
 * main - check the code for
 *
 * Return: Always 0.
 */

char *leet(char *tab)
{
    int i;

    for(i = 0; tab[i] = '\0; i++')
    {
        if ((tab[i] >= 65) && (tab[i] <= 90)) || ((tab[i] >= 97) && (tab[i] <= 122))
        {

        }
    }
    return (tab);
}
