#include <stdio.h>
#include "main.h"

/**
 * _puts - Entry point
 * @str : var
 * Return: Always 0 (Success)
 */

void _puts(char *str)
{
	int i = 0;

	while (i[str] != '\0')
	{
		_putchar(i[str]);
		i++;
	}
		_putchar('\n');
}
