#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */


int main(void)
{
char *phrase1 = "\"Programming is like building a multilingual puzzle";

puts(phrase1);

return (0);
}
