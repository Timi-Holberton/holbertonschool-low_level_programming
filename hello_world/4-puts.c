#include <stdio.h>

/*
 * main - Point d'entrée du programme.
 *
 * Cette fonction affiche une phrase sur la sortie standard.
 * La phrase compare la programmation à la construction d'un puzzle
 * multilingue. Chaque ligne de code respecte la limite de 80
 * caractères.
 *
 * Return: 0 indique le succès de l'exécution.
 */
int main(void)
{
    /* Déclaration et initialisation de la chaîne de caractères */
    char *phrase1 = "\"Programming is like building a multilingual puzzle";

    /* Affichage de la phrase sur la sortie standard */
    puts(phrase1);

    /* Retourne 0 pour signaler une exécution réussie */
    return (0);
}
