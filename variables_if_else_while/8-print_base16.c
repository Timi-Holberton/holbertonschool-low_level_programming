#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int hexa;

	for (hexa = 0; hexa <= 16; hexa++)
	{
		putchar(hexa);
	}
	putchar('\n');

	return (0);
}
